NAME:
  Package Changes Analyzer (pkgdiff) - a tool for analyzing changes in Linux software packages (RPM, DEB, TAR.GZ, etc).

INSTALL:
  sudo perl Makefile.pl -install --prefix=/usr

USAGE:
  pkgdiff -old OLD.rpm -new NEW.rpm

ADV. USAGE:
  For advanced usage, see output of --help option